Run the plugin management script with the given arguments.

Execute this bash command:

```bash
bash "$CLAUDE_PROJECT_DIR/.claude/scripts/plugin.sh" $ARGUMENTS
```

Report the output to the user. If the command fails, show the error message clearly.
